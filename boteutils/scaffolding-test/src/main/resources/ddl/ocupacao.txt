CREATE TABLE "OSCIP"."OCUPACAO" (
	"ID_OCUPACAO" NUMBER  NOT NULL ENABLE ,
	"CD_OCUPACAO" VARCHAR2(10 CHAR)  NOT NULL ENABLE ,
	"EN_PRINCIPAL" VARCHAR2(8 CHAR)  NOT NULL ENABLE ,
	"EN_POSSUI_COMPROVANTE" VARCHAR2(8 CHAR)  NOT NULL ENABLE ,
	"DT_DATA_INICIO_OCUPACAO" DATE  NOT NULL ENABLE ,
	"ID_CLIENTE" NUMBER  NOT NULL ENABLE,
	CONSTRAINT "OCUPACAO_PK" PRIMARY KEY ("ID_OCUPACAO")
);
CREATE SEQUENCE SEQ_OCUPACAO MINVALUE 1 MAXVALUE 9999999999 START WITH 1 INCREMENT BY 1 NOCACHE CYCLE;
ALTER TABLE "OSCIP"."OCUPACAO" ADD CONSTRAINT OCUPACAO_CLIENTE_FK FOREIGN KEY (ID_CLIENTE) REFERENCES CLIENTE(ID_CLIENTE);