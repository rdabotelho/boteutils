CREATE TABLE "OSCIP"."LOCALIZACAO" (
	"ID_LOCALIZACAO" NUMBER  NOT NULL ENABLE ,
	"EN_CONSULTA_CEP" VARCHAR2(8 CHAR)  NOT NULL ENABLE ,
	"EN_INDICATIVO_ZONA_URBANA" VARCHAR2(8 CHAR)  NOT NULL ENABLE ,
	"EN_TIPO_ENDERECO" VARCHAR2(16 CHAR)  NOT NULL ENABLE ,
	"EN_TIPO_COMPROVANTE" VARCHAR2(28 CHAR)  NOT NULL ENABLE ,
	"NU_CEP" VARCHAR2(8 CHAR)  NOT NULL ENABLE ,
	"TX_LOGRADOURO" VARCHAR2(60 CHAR)  NOT NULL ENABLE ,
	"NU_ENDERECO" VARCHAR2(10 CHAR) ,
	"NO_UF" VARCHAR2(2 CHAR)  NOT NULL ENABLE ,
	"NO_CIDADE" VARCHAR2(60 CHAR)  NOT NULL ENABLE ,
	"TX_COMPLEMENTO" VARCHAR2(60 CHAR) ,
	"TX_PERIMETRO" VARCHAR2(60 CHAR) ,
	"NO_BAIRRO" VARCHAR2(60 CHAR)  NOT NULL ENABLE ,
	"NU_TEMPO_UTILIZACAO" NUMBER  NOT NULL ENABLE ,
	"EN_ENDERECO_CORRESPONDENCIA" VARCHAR2(8 CHAR)  NOT NULL ENABLE ,
	"EN_IMOVEL_PROPRIO" VARCHAR2(8 CHAR)  NOT NULL ENABLE ,
	"ID_CLIENTE" NUMBER  NOT NULL ENABLE,
	CONSTRAINT "LOCALIZACAO_PK" PRIMARY KEY ("ID_LOCALIZACAO")
);
CREATE SEQUENCE SEQ_LOCALIZACAO MINVALUE 1 MAXVALUE 9999999999 START WITH 1 INCREMENT BY 1 NOCACHE CYCLE;
ALTER TABLE "OSCIP"."LOCALIZACAO" ADD CONSTRAINT LOCALIZACAO_CLIENTE_FK FOREIGN KEY (ID_CLIENTE) REFERENCES CLIENTE(ID_CLIENTE);