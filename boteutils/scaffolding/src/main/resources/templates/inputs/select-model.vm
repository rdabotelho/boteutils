			<h:panelGroup>
				<p:outputLabel for="${id}" value="${label}"/><br/>
	        		<p:selectOneMenu id="${id}" value="${value}">
	        			<f:selectItem itemLabel="" itemValue="#{null}" />
	        			<f:selectItems value="#{$field.namedItems}" var="item" itemLabel="#{$field.itemLabel}" itemValue="#{item}" />
	        		</p:selectOneMenu>
       		</h:panelGroup>